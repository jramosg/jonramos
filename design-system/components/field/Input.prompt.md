Text input on a sunken surface with a teal focus ring. Pair with `Field` for a label.

```jsx
<Input placeholder="you@example.com" />
<Input prefix="@" placeholder="jramosg" />
<Input invalid defaultValue="not-an-email" />
```

`prefix` renders a teal glyph inside the field; `invalid` switches to a clay border.
