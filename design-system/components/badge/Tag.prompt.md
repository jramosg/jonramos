Mono technology chip — reads as a code token. Use for tech stacks and labels.

```jsx
<Tag lead=":">Clojure</Tag>
<Tag>ClojureScript</Tag>
<Tag accent href="https://datomic.com">Datomic</Tag>
```

Pass `accent` for teal emphasis, `lead` for a small teal glyph, `href` to make it a link.
