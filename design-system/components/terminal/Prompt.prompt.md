The signature terminal line. Use as a section eyebrow, a hero flourish, or a status line.

```jsx
<Prompt sigil="$" cursor boxed>whoami</Prompt>
<Prompt sigil="›">git push origin master</Prompt>
```

`boxed` wraps it in a sunken chip; `cursor` adds the blinking teal caret.
